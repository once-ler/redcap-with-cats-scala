redcap {
  http {
    local {
      url = "http://localhost"
      token = ""
    }
  }
  db {
    local {
      url="jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=MSSQLServer"
      user="sa"
      password=""
      driver="org.h2.Driver"
      connections = {
        poolSize = 10
      }
    }
  }
}
